pipeline {
 agent any
 stages {
  stage('SCM') {
   steps {
    echo 'Got jenkinsfile from SCM'
   }
  }
  stage('Building') {
   steps {
    echo 'Building the code'
   }
  }
  stage('Testing') {
   steps {
    echo 'Tesing the code'
    }
  }
  stage('Deploying') {
   steps {
    echo 'Deploying the code'
   }
  }
 }
}
