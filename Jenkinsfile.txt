pipeline {
 agent any
 stages {
  stage('Preparing') {
   steps {
    echo 'preparing the code'
   }
  }
  stage('Building') {
   steps {
    echo 'Building the code'
   }
  }
  stage('Testing') {
   steps {
    echo 'Tesing the code'
    }
  }
  stage('Deploying') {
   steps {
    echo 'Deploying the code'
   }
  }
 }
}
